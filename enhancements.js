import{initializeApp,getApps}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-app.js';
import{getFirestore,doc,getDoc}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-firestore.js';
const $=id=>document.getElementById(id),cfg=await(await fetch('/__/firebase/init.json')).json(),fb=getApps()[0]||initializeApp(cfg),db=getFirestore(fb),root=doc(db,'gestionale','dati');
document.head.insertAdjacentHTML('beforeend','<style>.doctorFilter{display:flex;gap:5px;align-items:center;flex-wrap:wrap;margin-left:auto;border-left:1px solid #d7e0ea;padding-left:10px}.doctorFilterLabel{font-size:12px;font-weight:700;color:#334155}.doctorFilter button{padding:5px 8px;font-size:11px}.doctorFilter button.active{background:#1d5b93;color:#fff;border-color:#1d5b93}.doctorHighlighted{box-shadow:0 0 0 3px #f59e0b!important;border-color:#d97706!important;font-weight:700!important;position:relative;z-index:2}</style>');
let highlightedDoctor='';
function applyDoctorHighlight(){document.querySelectorAll('#schedule select').forEach(s=>s.classList.toggle('doctorHighlighted',!!highlightedDoctor&&s.value===highlightedDoctor))}
async function buildDoctorFilter(){let toolbar=document.querySelector('#turni .toolbar');if(!toolbar)return;let old=$('doctorFilter');if(old)old.remove();let snap=await getDoc(root),x=snap.exists()?snap.data():{},names=(x.doctors||[]).filter(d=>d.active).map(d=>d.name).filter(Boolean);if(!names.length)return;let box=document.createElement('div');box.id='doctorFilter';box.className='doctorFilter';box.innerHTML='<span class="doctorFilterLabel">Evidenzia:</span>';let all=document.createElement('button');all.type='button';all.textContent='Tutti';all.className=highlightedDoctor?'':'active';all.onclick=()=>{highlightedDoctor='';buildDoctorFilter();applyDoctorHighlight()};box.appendChild(all);for(const n of names){let b=document.createElement('button');b.type='button';b.textContent=n;if(n===highlightedDoctor)b.className='active';b.onclick=()=>{highlightedDoctor=n;buildDoctorFilter();applyDoctorHighlight()};box.appendChild(b)}toolbar.appendChild(box);applyDoctorHighlight()}
function start(){buildDoctorFilter();let schedule=$('schedule');if(schedule)new MutationObserver(()=>applyDoctorHighlight()).observe(schedule,{childList:true,subtree:true});$('month')?.addEventListener('change',()=>setTimeout(buildDoctorFilter,100));}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();setTimeout(buildDoctorFilter,800);setTimeout(buildDoctorFilter,2000);