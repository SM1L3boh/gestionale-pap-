import{initializeApp,getApps}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-app.js';
import{getFirestore,doc,getDoc,updateDoc}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-firestore.js';
const cfg=await(await fetch('/__/firebase/init.json')).json(),fb=getApps()[0]||initializeApp(cfg),db=getFirestore(fb),root=doc(db,'gestionale','dati');
function applyManualDefaults(){document.querySelectorAll('#schedule select[data-k]').forEach(s=>{const k=s.dataset.k||'';if(!(k.includes('|ferie|')||k.includes('|guardia|')||k.includes('|giorno|')))return;if(![...s.options].some(o=>o.value==='NESSUNO')){const o=document.createElement('option');o.value='NESSUNO';o.textContent='NESSUNO';s.insertBefore(o,s.options[1]||null)}if(!s.value)s.value='NESSUNO'})}
async function clearAutomaticDraft(e){e.preventDefault();e.stopImmediatePropagation();const month=document.getElementById('month')?.value;if(!month)return;if(!confirm(`Cancellare la bozza automatica di ${month}? Saranno conservati turni manuali, ferie e medici a contratto.`))return;await new Promise(r=>setTimeout(r,450));const snap=await getDoc(root),x=snap.exists()?snap.data():{},schedule={...(x.schedule||{})},manual=new Set(x.manualKeys||[]),generated=new Set(x.generatedKeys||[]),extra=new Set(x.extraKeys||[]),contracts=new Set((x.doctors||[]).filter(d=>d.active&&d.cat==='Contratto').map(d=>d.name)),baseline=x.savedStates?.[month]||{},keep={};
 // Recupero robusto: anche se SALVA STATO e' stato premuto dopo Genera bozza,
 // una chiave ancora marcata generated non entra nello stato manuale.
 for(const[k,v]of Object.entries(baseline)){if(!k.startsWith(month+'-'))continue;const svc=k.split('|')[1],manualSvc=['guardia','giorno','esami','ferie'].includes(svc);if(manual.has(k)||manualSvc||contracts.has(v)||(!generated.has(k)&&v==='NESSUNO'))keep[k]=v}
 // Le modifiche manuali fatte dopo l'ultimo SALVA STATO hanno precedenza.
 for(const[k,v]of Object.entries(schedule)){if(!k.startsWith(month+'-'))continue;const svc=k.split('|')[1],manualSvc=['guardia','giorno','esami','ferie'].includes(svc);if(manual.has(k)||manualSvc||contracts.has(v))keep[k]=v}
 for(const k of Object.keys(schedule))if(k.startsWith(month+'-'))delete schedule[k];for(const[k,v]of Object.entries(keep))schedule[k]=v;for(const k of [...generated])if(k.startsWith(month+'-'))generated.delete(k);for(const k of [...extra])if(k.startsWith(month+'-'))extra.delete(k);const savedStates={...(x.savedStates||{}),[month]:keep};const sync=document.getElementById('sync');try{if(sync)sync.textContent='Cancellazione bozza…';await updateDoc(root,{schedule,savedStates,generatedKeys:[...generated],extraKeys:[...extra],updatedAt:new Date().toISOString()});if(sync){sync.textContent='● Turni manuali ripristinati';sync.className='status online'}alert(`Bozza ${month} cancellata. Conservati soltanto i turni manuali, ferie e medici a contratto.`);location.reload()}catch(err){if(sync)sync.textContent='Errore cancellazione';alert('Errore durante la cancellazione: '+err.message)}}
function installAuthoritativeClear(){const old=document.getElementById('clearDraft');if(!old||old.dataset.authoritativeClear)return;const b=old.cloneNode(true);b.dataset.authoritativeClear='1';b.onclick=null;old.replaceWith(b);b.addEventListener('click',clearAutomaticDraft)}
function startLeaveDefaults(){applyManualDefaults();installAuthoritativeClear();const schedule=document.getElementById('schedule');if(schedule)new MutationObserver(()=>applyManualDefaults()).observe(schedule,{childList:true,subtree:true});document.getElementById('month')?.addEventListener('change',()=>setTimeout(applyManualDefaults,100))}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',startLeaveDefaults);else startLeaveDefaults();setTimeout(()=>{applyManualDefaults();installAuthoritativeClear()},400);setTimeout(()=>{applyManualDefaults();installAuthoritativeClear()},1200);
